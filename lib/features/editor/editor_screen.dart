import 'package:flutter/material.dart';
class EditorScreen extends StatelessWidget { final String? imagePath; const EditorScreen({super.key, this.imagePath}); @override Widget build(BuildContext c) => const Scaffold(body: Center(child: Text('Editor'))); }

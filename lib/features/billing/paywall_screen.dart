import 'package:flutter/material.dart';
class PaywallScreen extends StatelessWidget { const PaywallScreen({super.key}); @override Widget build(BuildContext c) => Scaffold(appBar: AppBar(title: const Text('PaywallScreen')), body: const Center(child: Text('Feature scaffold'))); }
class HistoryScreen extends StatelessWidget { const HistoryScreen({super.key}); @override Widget build(BuildContext c) => const Scaffold(body: Center(child: Text('History'))); }
class BatchScreen extends StatelessWidget { const BatchScreen({super.key}); @override Widget build(BuildContext c) => const Scaffold(body: Center(child: Text('Batch'))); }
